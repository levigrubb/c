#include <stdio.h>

/*
4. Traverse and array using pointers instead of indices
    use pointer arithmetic (ptr + i) to access elements
*/

/*
5. Understanding that array names are pointers.
    Write a function that prints or modifies an array passed as a pointer argument.
*/

/*
6. Pass an array to a function using pointers.
    Write a function that prints or modifies an array passed as a pointer argument.
*/