#include <stdio.h>

/*
17. Use gdb to inspect pointers
    Set breakpoints and use `print &var`, `print ptr`, `x/4wx ptr` to visulaize memory
*/

/*
18. Deliberately cause pointer errors
    e.g., dereference a NULL pointer or forget to `free()`
*/

