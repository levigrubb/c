#include <stdio.h>

/*
12. Create a struct and a pointer to that struct
    Access members through both `(*ptr).member` and `ptr->member`
*/

/*
13. Dynamically allocate a struct and modify it's behavior
    Understand how malloc and -> work together.
*/