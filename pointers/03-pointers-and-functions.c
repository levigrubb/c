#include <stdio.h>

/*
7. Use pointers to return multiple values from a function
    e.g., return both quotient and remainder via pointer arguments.
*/

/*
8. Pass a pointer to a function and modify the original variable
    Watch how changing the dereferenced value inside the function affects the caller.
*/